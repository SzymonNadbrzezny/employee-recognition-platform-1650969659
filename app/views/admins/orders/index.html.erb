<div class='title'>Orders of <%= employee.email.to_s %> </div>
<table>
  <thead>
    <tr>
      <th>Order NR.</th>
      <th>Reward Title</th>
      <th>Price</th>
      <th>Time of Purchase</th>
      <th colspan="1"></th>
    </tr>
  </thead>
  <tbody>
    <% orders.each do |order| %>
    <% reward = Reward.from_json(order.reward)%>
      <tr>
        <td><%= order.id %></td>
        <td><%= reward.title %></td>
        <td><%= reward.price %></td>
        <td><%= order.created_at %></td>
        <td><%= link_to "Show", admins_employee_order_path(order) %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<br>

